# 项目约束

## 案例目录

- 新增或生成的可运行案例统一放在 `examples/<案例名称>/` 下，包括案例所需的 Python、HTML、JavaScript 和静态资源。
- 不要在仓库根目录新增案例入口文件或临时演示脚本。根目录只保留项目配置、构建脚本、文档和正式接口文件。
- 自动化测试放在 `tests/`，正式 Rust 实现放在 `src/`；不要把它们当作案例放入 `examples/`。
- 新增案例时，在 `README.md` 中写明对应路径和运行命令。

## Python 代码

- 生成或修改的 Python 代码必须通过 `ruff` 的 `ANN` 规则检查和 `ty` 检查。
- 项目最低支持 Python 3.9，案例代码也必须兼容 Python 3.9。
